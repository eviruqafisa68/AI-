import {PageHead} from '../components/common/UI';import {BookOpen,MessageCircle,Video,ArrowRight} from 'lucide-react';
export default function HelpPage(){return <><PageHead title="帮助中心" desc="了解从创意输入到成片导出的完整制作方法。"/><div className="agent-grid">{[['新手快速入门','用 10 分钟建立第一个AI短片项目',BookOpen],['工作流视频课','按阶段掌握剧本、资产、分镜与生成',Video],['产品支持','提交建议与查看常见问题',MessageCircle]].map(([n,d,I])=>{const Icon=I as typeof BookOpen;return <button key={String(n)} onClick={()=>alert('演示模式：帮助内容将在知识库接入后开放')}><Icon/><h3>{String(n)}</h3><p>{String(d)}</p><b>查看内容 <ArrowRight/></b></button>})}</div><article className="panel faq"><h2>常见问题</h2>{['第一阶段会真实调用AI模型吗？','文件是否会上传到云端？','如何部署到 Vercel？','下一阶段将接入哪些服务？'].map((x,i)=><details key={x}><summary>{x}</summary><p>{['不会。所有生成与分析均为明确标注的前端演示任务。','不会。文件选择只读取名称等本地信息，不向服务端发送。','导入 Git 仓库，设置三个 VITE 环境变量并使用 npm run build。','鉴权、项目持久化、对象存储、任务队列以及文本、图像、视频、音频模型 Provider。'][i]}</p></details>)}</article></>}
