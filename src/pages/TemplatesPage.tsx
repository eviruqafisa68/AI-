import {PageHead} from '../components/common/UI';import {templates} from '../data/mockData';import {Link} from 'react-router-dom';import {ArrowRight,Users} from 'lucide-react';
export default function TemplatesPage(){return <><PageHead title="模板中心" desc="从经过设计的流程、参数与检查清单开始项目。"/><div className="template-grid">{templates.map(t=><article key={t.id}><div style={{background:`linear-gradient(145deg,${t.color},#111721)`}}><small>{t.scene}</small><b>{t.name.slice(0,2)}</b></div><h3>{t.name}</h3><p>{t.ratio} · {t.duration} · 剧本 → 资产 → 分镜 → 成片</p><span><Users size={13}/>{t.users.toLocaleString()} 人使用（模拟）</span><footer><button>查看详情</button><Link className="primary" to={`/projects/new?template=${t.id}`}>使用模板 <ArrowRight size={14}/></Link></footer></article>)}</div></>}
