import {useState} from 'react';import {PageHead,DemoBanner} from '../components/common/UI';import {records} from '../data/mockData';import {RefreshCw,Trash2} from 'lucide-react';import {useAppStore} from '../stores/appStore';
export default function GenerationsPage(){const [filter,setFilter]=useState('全部');const toast=useAppStore(s=>s.setToast);const rows=filter==='全部'?records:records.filter(r=>r.status===filter);return <><PageHead title="生成记录" desc="审计全部图像、视频、音频与分析任务的参数、状态和结果。"/><DemoBanner/><div className="filter-tabs">{['全部','排队中','生成中','成功','失败','已取消'].map(x=><button className={filter===x?'active':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div><div className="table-wrap"><table><thead><tr>{['任务编号','项目 / 镜头','类型 / 模型','状态','模拟积分','操作人员','操作'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.id}</b></td><td>{r.project}<small>{r.shot}</small></td><td>{r.type}<small>{r.model}</small></td><td><span className={`status ${r.status}`}>{r.status}</span></td><td>{r.points}</td><td>{r.operator}</td><td><button className="icon" onClick={()=>toast(`${r.id} 已重新加入演示队列`)} aria-label="重试"><RefreshCw/></button><button className="icon" onClick={()=>toast('已从当前视图删除（演示）')} aria-label="删除"><Trash2/></button></td></tr>)}</tbody></table></div></>}
