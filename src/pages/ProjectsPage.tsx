import {Plus,Search,SlidersHorizontal} from 'lucide-react';import {Link} from 'react-router-dom';import {PageHead} from '../components/common/UI';import {ProjectCard} from '../components/project/ProjectCard';import {useAppStore} from '../stores/appStore';
export default function ProjectsPage(){const projects=useAppStore(s=>s.projects);return <><PageHead title="项目中心" desc="统一管理剧本、资产、分镜、生成任务、时间线与版本。" actions={<Link className="primary" to="/projects/new"><Plus size={16}/>新建项目</Link>}/><div className="toolbar"><label><Search size={16}/><input aria-label="搜索项目" placeholder="搜索项目名称或负责人"/></label><button className="secondary"><SlidersHorizontal size={16}/> 全部阶段</button></div><div className="project-grid">{projects.map(p=><ProjectCard p={p} key={p.id}/>)}</div></>}
