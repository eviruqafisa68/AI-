import {ArrowUpRight,MoreHorizontal} from 'lucide-react'; import {Link} from 'react-router-dom'; import type {Project} from '../../types'; import {Progress} from '../common/UI';
export function ProjectCard({p}:{p:Project}){return <article className="project-card"><div className="cover" style={{background:`linear-gradient(145deg,${p.color},#10151e)`}}><span>{p.type}</span><b>{p.name.slice(0,2)}</b></div><div className="card-body"><div className="card-title"><h3>{p.name}</h3><button className="icon" aria-label="更多操作"><MoreHorizontal size={18}/></button></div><p>{p.type} · {p.ratio} · {p.stage}</p><Progress value={Math.round(p.done/p.shots*100)}/><div className="meta"><span>{p.done}/{p.shots} 镜头</span><span>{p.updated}</span></div><footer><span>{p.owner} 负责</span><Link className="link" to={`/projects/${p.id}`}>继续制作 <ArrowUpRight size={14}/></Link></footer></div></article>}
